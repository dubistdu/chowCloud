
.container-fluid.index
  -if @restaurants.empty?
    .jumbotron
      %h1 Sorry! No Restaurants Found!
  - else
    .row.center
      - @restaurants.each do |restaurant|
        .col-lg-4.col-md-6.col-sm-6.col-xs-12
          = link_to restaurant do
            .restaurantframe
              .image-container.img
                .caption
                  Hello
                  - if restaurant.pictures.any?
                    = image_tag attachment_url(restaurant.pictures.first, :image), size: "400x270", class: "restaurant_image_thumb"
                  - else
                    = image_tag("placeholder.jpg", size: "400x270", class: "restaurant_image_thumb")
              .restaurantprofile
                .container
                  %h3
                    = restaurant.name
                  %p
                    = restaurant.city + ","
                    = restaurant.state


-# .indexx
-#
-#   - if @restaurants.empty?
-#     -# edit at some point
-#     .jumbotron
-#       %h1 Sorry! No Restaurants Found!
-#   - else
-#     .row.form-group
-#       - @restaurants.each do |restaurant|
-#         .col-xs-12.col-md-6
-#           .panel.panel-default
-#         .panel-heading
-#           %h3 You can even have a Panel Title
-#         .panel-image.hide-panel-body
-#         = link_to restaurant do
-#           - if restaurant.pictures.any?
-#             = image_tag attachment_url(restaurant.pictures.first, :image), size: "400x300", class: "panel-image-preview"


    -# .table-responsive
    -# %table.table.table-striped.table-bordered.table-hover
      -# %thead
      -#   %tr
      -#     %th Name
      -#     %th Address
      -#     %th City
      -#     %th State
      -#     %th Phone
      -#     %th Website
      -#     %th Hours
      -#     %th Price
      -#     %th Cuisine
      -#     %th Menu
      -#     %th Images
      -#     %th{colspan: 3}

      -# %tbody
      -#   - @restaurants.each do |restaurant|
      -#
      -#     %tr

            -# %td= restaurant.name
            -# %td= restaurant.address
            -# %td= restaurant.city
            -# %td= restaurant.state
            -# %td= restaurant.phone
            -# %td= restaurant.website
            -# %td= restaurant.hours
            -# %td= dollar_rating(restaurant.price)
            -# %td= restaurant.cuisine
            -# %td
            -#   TODO, show menu PDF
            %td
              = link_to restaurant do
                - if restaurant.pictures.any?
                  = image_tag attachment_url(restaurant.pictures.first, :image), size: "180x100"
            %td

              = link_to restaurant do
                %span.glyphicon.glyphicon-ok
            %td
              = link_to edit_restaurant_path(restaurant) do
                %span.glyphicon.glyphicon-pencil
            %td
              = link_to restaurant, data: { confirm: 'Are you sure?' }, method: :delete do
                %span.glyphicon.glyphicon-remove
